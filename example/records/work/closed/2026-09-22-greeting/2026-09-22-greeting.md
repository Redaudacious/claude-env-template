# The greeting — execution plan

> **For whoever executes:** the plan is executed in a **new** session, with the
> `execute` skill, on the model and with the effort below. The steps have boxes
> (`- [ ]`); **they are not ticked**: the state of the work item lives in
> `records/work/INDEX.md`.

**Execution:** claude-sonnet-5, effort high — two small files, with the code already written here; nothing to investigate
**Closing:** Opus review — the request asks for it: the exercise goes through all four sessions
**Branch:** `execute/greeting`

**The goal:** `greeting.sh` prints `hello, <name>`, exits with 2 on a call without a
name, and has a test that the framework's `./test` runs.

**Spec:** [2026-09-22-greeting-design.md](2026-09-22-greeting-design.md).

## Constraints for every task

- The project's root is `projects/exercise`; the commands are given from there.
- The test is written before the script and has to fail once, before it.
- The commits are in English, say why, and carry no `Co-Authored-By`.
- Nothing enters `main`: the branch is pushed at the end, and the review merges it.

---

### Task 0: the state and the branch

**Who:** session — no local worker: the exercise must work on a machine without one too.
**Verifier:** the branch is `execute/greeting`, started from an up-to-date `main`.

- [ ] **Step 1: the state of the repository**

```bash
git fetch --quiet && git status -sb && git status --porcelain
```

Expected: `## main`, up to date with its origin or without an origin, and nothing
uncommitted.

- [ ] **Step 2: the branch**

```bash
git switch -c execute/greeting
```

---

### Task 1: the test, then the script

**Who:** session — no local worker: the exercise must work on a machine without one too.
**Verifier:** `bash tests/test-greeting.sh` prints `✓ 2 tests passed`.

**Files:** create `tests/test-greeting.sh`, `greeting.sh`.

- [ ] **Step 1: the test, which fails because the script does not exist**

`tests/test-greeting.sh`, with the execute bit (`chmod 755`):

```bash
#!/bin/bash
# test-greeting.sh — the test of greeting.sh: the greeting, and the use without a name.
set -u
S="$(cd "$(dirname "$0")/.." && pwd)/greeting.sh"
[ "$("$S" Ana 2>/dev/null)" = "hello, Ana" ] || { echo "✗ the greeting is missing"; exit 1; }
"$S" >/dev/null 2>&1
[ $? -eq 2 ] || { echo "✗ without a name, the code must be 2"; exit 1; }
echo "✓ 2 tests passed"
```

```bash
bash tests/test-greeting.sh; echo "code=$?"
```

Expected: `✗ the greeting is missing`, `code=1`.

- [ ] **Step 2: the script**

`greeting.sh`, with the execute bit:

```bash
#!/bin/bash
# greeting.sh — prints "hello, <name>". Without a name, says how it is used and exits with 2.
[ $# -eq 1 ] || { echo "usage: greeting.sh <name>" >&2; exit 2; }
echo "hello, $1"
```

- [ ] **Step 3: the test, which passes**

```bash
bash tests/test-greeting.sh && ./greeting.sh Ana
```

Expected: `✓ 2 tests passed`, then `hello, Ana`.

- [ ] **Step 4: the commit**

```bash
git add greeting.sh tests/test-greeting.sh && git commit -m "The greeting has its test first, then the script, so that a call without a name fails with a sign instead of greeting nobody"
```

---

### Task 2: the row in the impact map

**Who:** session — no local worker: the exercise must work on a machine without one too.
**Verifier:** `bin/check-maps.sh` from the workspace root runs the row of
`greeting.sh`, with `failed: 0`.

**Files:** modify `IMPACT.md`.

- [ ] **Step 1: the row.** In the "Verifiable" table of `IMPACT.md`, the template's row
  for `README.md` is replaced by one for `greeting.sh`: what breaks is the greeting or
  code 2, it depends on the test, and the verification is
  `bash tests/test-greeting.sh >/dev/null`. The date of the measurement, `2026-09-22`.
  Under "Not verifiable", one line: no conceptual dependency.

- [ ] **Step 2: the verifier, then the commit**

```bash
../../bin/check-maps.sh | tail -1
```

Expected: `failed: 0`.

```bash
git add IMPACT.md && git commit -m "The map knows about greeting.sh, so that a change to it is caught by check-maps, not only by whoever remembers the test"
```

## What is NOT in this plan

- **The project's `CLAUDE.md`**: it has nothing to say that would cost if not known.
- **Other arguments** for the script, a `--help`, several names: the request does not
  ask for them.
