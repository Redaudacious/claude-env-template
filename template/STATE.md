# <project name> — state and the next session

> Rewritten on <date>, at the closing of the session in which <what was finished>.
>
> What closes **leaves** this file: system problems go to `records/journal/`, pitfalls to
> `records/pitfalls/`, the human's decisions to `records/decisions/`, decisions worth
> explaining to `docs/explanation/`, the rest is deleted — git keeps it anyway.
> A file that only grows is no longer read.
>
> ⚠ **This is the only file that the end-of-session command rewrites in full.**
> `README.md`, `CLAUDE.md` and `IMPACT.md` are not written automatically: the command
> reports what should be added to them and stops.

## The state, measured on <date>

<Only figures measured now. A figure that is too round is a ceiling, not a measurement.>

## What does not work

<Only open problems. The closed ones have already left.>

## Measurements still owed

<What was claimed without being verified. This list is what stops an assumption from
slipping into the next session as a fact.>

---

## The next session

### Read first, in this order

1. `CLAUDE.md` at the workspace root
2. `CLAUDE.md` and `IMPACT.md` in this project
3. <the rest>

### The work of the session

<What is done, in order.>

### What is NOT done in this session

<Just as important. Without it, the session spreads.>

### The first command

```
<command>
```

It must show: <what exactly>. If it does not show that, nothing else matters until it is
repaired.

### Questions already asked and answered — not asked again

- **<the question>** <the answer>
